import { Box } from '@mui/material';
import * as React from 'react';

const KatexOutputRender = () => (
    <Box id="katex-render" padding="20px">
    </Box>
)

export default KatexOutputRender;